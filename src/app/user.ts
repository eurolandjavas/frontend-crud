export class User {
    id?: number;
    firstName: string;
    lastName: string;
    city: string;
    age: number;
    // constructor(public id: number, public firstName: string, public lastName: string, public city: string, public age: number) {}
}
